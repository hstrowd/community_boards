<h1>Listing events</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Description</th>
    <th>Community</th>
    <th>Event Date</th>
    <th>Start Time</th>
    <th>End Time</th>
    <th>Cost</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @events.each do |event| %>
  <tr>
    <td><%= event.title %></td>
    <td><%= event.description %></td>
    <td><%= link_to event.community.name, event.community %></td>
    <td><%= event.start_time.strftime('%D') %></td>
    <td><%= event.start_time.strftime('%H:%M') %></td>
    <td><%= event.end_time.strftime('%H:%M') %></td>
    <td><%= event.cost %></td>
    <td><%= link_to 'Show', event %></td>
    <td><%= link_to 'Edit', edit_event_path(event) %></td>
    <td><%= link_to 'Destroy', event, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Event', new_event_path %> | <%= link_to 'Home', root_path %>
